import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Registros',
};

export default async function Page() {
  return (
    <main>
      <h1>Registros</h1>
    </main>
  );
}
